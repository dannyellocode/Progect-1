# Progect-1

## Tasks

- [ ] - Learn git basics `git clone`, `git pull`, `git push`, `git checkout` / `git switch`, `git add`
- [ ] - Create first Pull Request
    1. add files to git
    2. git push
    3. create pull request on github
